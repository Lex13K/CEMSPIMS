# CEMSPIMS does not require environment variables for the default offline pipeline.
# Place raw inputs under data/raw/ (see docs/DATA.md).
#
# If you later add WRDS API access or other secrets, copy this file to .env and fill in values.
# Never commit .env — it is listed in .gitignore.
